# pbac.ai

Source code for the pbac.ai website.

## Local preview

This repository currently ships as a static single-page site. To preview it locally, open
`index.html` in a browser or serve the repository root with any static file server before
deploying it to Cloudflare Pages.
